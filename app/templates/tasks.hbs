{{outlet}}
<h1>Task</h1>
<hr>
{{#each model as |task|}}
    <div class='well'>
        <h4>{{#link-to 'task.edit' task.id}} {{task.title}} {{/link-to}}</h4>
        <small>Due: {{format-date task.date}}</small>
        <p>{{task.description}}</p>
    </div> 
{{/each}}
